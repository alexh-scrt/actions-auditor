# actions-auditor
actions_auditor is a CLI security tool that scans GitHub Actions workflow YAML files for common misconfigurations and vulnerabilities, including overly permissive token scopes, exposed secrets in environment variables, use of third-party actions without pinned SHAs, script injection risks, and misconfigured pull-request triggers. It produces a prio
